name: Create Issue

on:
  push:
    paths:
      - 'docs/issues.md'

permissions:
  issues: write

jobs:
  issue:
    uses: AgentGG00/workflows/.github/workflows/create-issue.yml@main
    secrets: inherit
