## [0.1.1] - 2026-06-20

### 🚀 Features

- *(dexcom)* Add dexcom share api client
- *(db)* Add sqlite schema, db worker and polling logic
- *(keychain)* Add os keychain integration
- *(core)* Wire up db, logger, keychain and worker in app entry point
- *(backend)* Add db, error handler, keychain and worker modules
- *(frontend)* Add tailwind, css variables and system theme handler
- *(i18n)* Add multilingual support for de, en and jp
- *(wizard)* Add wizard screen 1 with sensor, region and requirements
- *(os)* Add tauri-plugin-os for system locale detection
- *(i18n)* Use OS locale for language detection via tauri-plugin-os
- *(wizard)* Add step 3 auth loading, external error feedback, OS locale detection, password UX
- Wizard step 4 + step 5 complete
- Implement handleFinish with worker start and window close
- Dynamic tray icon with live value, color, trend and first-start notification
- Add color scheme logic to tray with zone-based color resolution
- Load unit setting into AppState on startup for tray display
- Autostart implementation via tauri-plugin-autostart
- *(backend)* Typed error handling for dexcom api and worker
- *(frontend)* Display typed error codes as localized messages in wizard
- *(legal)* Add privacy policy, terms of use, and disclaimer (de/en/jp)
- *(backend)* Add legal document reading and acceptance storage
- *(wizard)* Add step 0 for legal document acceptance (privacy, terms, disclaimer)
- *(backend)* Add settings read/write commands and wizard status check
- *(frontend)* Add settings window with credentials change, legal document viewers, and routing
- *(backend)* Keep app running in background when settings window is closed
- *(backend)* Add self-updater with store/flatpak fallback
- *(frontend)* Wire update checker to backend with store/flatpak fallback UI

### 🐛 Bug Fixes

- Tauri-build use node 22 ci-build delete
- Create issue workflow
- *(ci)* Set projectPath, working directory, and write permissions for tauri-build

### 💼 Other

- Tauri + svelte project initialized
- Add issue for handling Dexcom LOW/HIGH string values in glucose readings
- Test
- Dexcom LOW/HIGH string values not handled
- Add mg/dL internal state refactor (frontend/backend) to issues.mds

### 🚜 Refactor

- Unified mg/dL as internal unit, single conversion point in db.rs

### 📚 Documentation

- Add handover document
- Update claude-übergabe.md
- Update project-status and claude-übergabe after wizard completion
- Update project-status and claude-übergabe after tray + autostart session
- Update project status and handoff notes for error handling
- Update project status and handoff notes to English, reflect legal/step0 work
- Update project status and handoff notes for settings window, tray rework, and window-close behavior
- Update project status and handoff notes for self-updater and distribution strategy
